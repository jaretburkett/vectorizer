{
  "name": "vectorizer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "cross-env": "^7.0.3",
    "gh-pages": "^3.2.3",
    "imagetracerjs": "^1.2.6",
    "rc-slider": "^9.7.4",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "svgson": "^5.2.1",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "publish": "cross-env PUBLIC_URL=/vectorizer/ NODE_ENV=production npm run build && node publish.js",
    "test": "react-scripts test",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,css,scss,md}\"",
    "eject": "react-scripts eject"
  },
  "prettier": "prettier-basic",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-basic": "^1.0.0"
  }
}
